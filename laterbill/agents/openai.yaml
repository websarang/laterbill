interface:
  display_name: "하다 만 일 종결반"
  short_description: "미완료 업무를 찾아 텍스트와 HTML 청구서로 자동 발행합니다"
  default_prompt: "Use $laterbill to scan my local AI conversations read-only, show the bill as text, and open the same bill as private HTML."

policy:
  allow_implicit_invocation: true
