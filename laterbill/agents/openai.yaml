interface:
  display_name: "하다 만 일 종결반"
  short_description: "로컬 AI 대화의 미완료 업무에 종결안 세 가지를 제시합니다"
  default_prompt: "Use $laterbill to track unfinished work in my local AI conversations and propose three evidence-grounded closure options."

policy:
  allow_implicit_invocation: true
